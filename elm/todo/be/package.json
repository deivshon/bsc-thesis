{
    "name": "todo",
    "version": "1.0.0",
    "description": "",
    "main": "src/index.ts",
    "scripts": {
        "build": "pnpm typecheck && pnpm bundle",
        "typecheck": "tsc --noEmit",
        "bundle": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=cjs --minify",
        "clean": "rm -rf dist"
    },
    "keywords": [],
    "author": "",
    "devDependencies": {
        "esbuild": "^0.23.1",
        "typescript": "^5.5.4"
    }
}
